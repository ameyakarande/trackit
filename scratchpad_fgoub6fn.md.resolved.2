# Debugging "Download PO" Button

## Plan
1. [x] Navigate to http://localhost:3001/
2. [x] Login as 'admin' / 'admin123' (Logged in as Company Admin)
3. [x] Find a 'PO Issued' quotation (Found and expanded)
4. [x] Open browser console and click 'Download PO'
5. [ ] Check for console errors and report them (No errors found in console so far)
6. [x] Check if `/logo.png` is reachable (Status 200)
7. [ ] Verify if PDF generation was attempted

### Findings
- `/logo.png` is reachable (HTTP 200).
- `Download PO` button was clicked twice (once via pixel, once via JS), but no console errors appeared.
- "jsPDF availability: false" in console (possibly because it's bundled and not on `window`).
- No visible errors or feedback on the page when the button is clicked.
